using UnityEngine;

public class UI_Sounds : MonoBehaviour
{
    public AudioClip buttonClick;
    public AudioClip coolerButtonClick;
    // Start is called before the first frame update


    // Update is called once per frame

}
